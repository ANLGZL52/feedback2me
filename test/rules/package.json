{
  "name": "feedbacktome-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Firestore Security Rules — feedback lifecycle invariant testleri (emulator).",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.3.0"
  }
}
